/**
 * @deprecated The original scroll-step pin system has been superseded by the
 * cinematic journey. Pins are now defined in `journey.ts`, rendered/positioned
 * by `overlay.ts`, and driven by the pallet-follow camera in `scene3d.ts`.
 *
 * This file is intentionally left as an empty module to keep the project free
 * of dead code while preserving import history in version control.
 */
export {};
